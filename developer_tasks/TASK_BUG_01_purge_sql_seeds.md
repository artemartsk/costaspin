# 🐞 BUG REPORT: Hardcoded Demo Seed Data overwriting Real Backend

**Status:** TODO / URGENT  
**Assignee:** Backend Developer  
**Priority:** 🔴 HIGH  

## Описание проблемы (Issue Details)
Во время интеграции и тестирования фронтенда мы обнаружили, что в интерфейсе (Calendar & Practitioners) отображаются вымышленные врачи (Dr. James Wilson, Sarah Chen и т.д.). 

Фронтендер корректно выполнил `TASK_02` (удалил `src/lib/mock-data.ts`), **ОДНАКО**, бэкенд команда оставила SQL-скрипты сидирования (`002_seed_data.sql` и `008_demo_seed.sql`) прямо в директории `supabase/migrations/`. 
Из-за этого при локальном запуске (или деплое) эти миграции замусоривают таблицы фиктивными данными, игнорируя тот факт, что в БД **уже заведены реальные врачи и пациенты**.

## Требуемые действия (Action Items)
1. Я (PM) только что принудительно переименовал эти файлы в `.disabled`, чтобы они перестали выполняться и перетирать реальную картину.
2. Программисту: **Никогда не заливайте тестовые демо-сиды в папку продакшен `migrations/`**. Если вам нужны моковые данные для локальной работы, используйте файл `supabase/seed.sql` (который запускается только при `npx supabase start` или `db reset`), а не создавайте инкрементальные миграции `002_...`, `008_...`, которые могут улететь на staging/prod!
3. Удалите эти файлы полностью из кодовой базы или перенесите их содержимое в единый игнорируемый `supabase/seed.sql` для личной локальной разработки.
4. Проверить, не ушли ли эти "Доктора Вилсоны" на реальный Staging проект в Supabase. Если ушли — сделать миграцию по удалению `DELETE FROM practitioners WHERE id IN (...)`.

---
**Штрафные баллы в карму программисту начислены.**  
**Created:** 02.04.2026 / *Antigravity PM Agent*
